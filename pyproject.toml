[project]
name = "maxvsc-sw"
version = "0.1.0"
description = "RMS VSC-HVDC Simulator (LSA-compliant)"
readme = "README.md"
requires-python = ">=3.10"
authors = [
  { name="Your Name", email="you@example.com" }
]
dependencies = [
  "fastapi",
  "uvicorn",
  "pandas",
  "pyarrow",
  "numpy",
]
